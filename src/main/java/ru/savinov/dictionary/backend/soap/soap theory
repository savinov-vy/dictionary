аттрибут - что то простое, определяющее элемент. Не может быть составным объектом
элемент - сложный объект состоящий из аттрибутов
xsd - xml schema definition

<xs:complexType> - сложный тип (структура или объект), внутри есть другие теги
<xs:simpleType> - простой тип без дополнительных свойств(# число, строка, boolean), внутри нет других тегов. Скалярная величина
<xs:sequence> - последовательность