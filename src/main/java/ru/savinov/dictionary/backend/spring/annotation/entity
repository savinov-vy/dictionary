rest_springboot_h2
    @Entity - аннотация указывает что этот класс является JPA сущностью. У каждой сущности не являющейся вложенной дол-
    жен иметь primary key обозначенный аннотацией @Id


    @GeneratedValue (strategy = GenerationType.AUTO) - аннотация служит для обеспечения генерации уникальных ключей
    strategy = GenerationType.AUTO - генерация ключей будет обеспечиваться на стороне бэкенда или базы данных по выбору
    спринг

    strategy = GenerationType.IDENTITY - генерация ключей будет обеспечиваться на стороне базы данных

